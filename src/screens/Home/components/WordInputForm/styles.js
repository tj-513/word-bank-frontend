import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    textInput:{
        padding: 4,
        borderRadius: 3,
        borderWidth: 1,
        borderColor: 'gray',
        marginTop:4,
        marginBottom:4,
    }
  });

  export default styles;