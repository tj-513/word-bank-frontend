import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  sectionHeader: {
    paddingTop: 10,
    paddingBottom: 3

  },
  sectionHeaderText:{
    fontSize: 14,
    fontWeight: 'bold'
  }
});

export default styles;
