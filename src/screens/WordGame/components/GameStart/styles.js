import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  title: {
    fontSize: 20,
    fontWeight: '600',
    alignSelf: 'center'
  },
  message: {
      paddingTop: 20,
      textAlign: 'center'
  }
});

export default styles;
