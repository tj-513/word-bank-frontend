import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  message: {
    fontSize: 20,
    fontWeight: '600',
    alignSelf: 'center'
  },
  activityIndicator: {
      paddingTop: '25%'
  }
});

export default styles;
